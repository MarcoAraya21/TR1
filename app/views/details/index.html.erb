<% if user_signed_in? %> 

<p id="notice"><%= notice %></p>

<% if current_user.usertype_id == 1%>

<h1>Productos en venta actual</h1>

<table>
  <thead>
    <tr>
      <th>N° Venta</th>
      <th>Producto</th>
      <th>Categoria</th>
      <th>Cantidad</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody align="center">
    <% @details.where(:sale_id => Sale.last).each do |detail| %>
      <tr>
        <td><%= detail.sale.id %></td>
        <td><%= detail.product.Nombre %></td>
        <td><%= detail.product.Pcategory.Nombre %></td>
        <td><%= detail.Cantidad %></td>
        <td><%= link_to 'Mostrar', detail %></td>
        <td><%= link_to 'Editar', edit_detail_path(detail) %></td>
        <td><%= link_to 'Borrar', detail, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Ingresar Producto a venta actual', new_detail_path %>
<br>
<%= link_to 'Ir a ventas', sales_path %>
<% end %>
<% else %>
 <h1> No Tienes los permisos para ingresar.</h1>
 <b>Favor de contactar al administrador.</b>
 <% end %>