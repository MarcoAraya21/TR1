<%= form_for(@reparation) do |f| %>
  <% if @reparation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reparation.errors.count, "error") %> prohibited this reparation from being saved:</h2>

      <ul>
      <% @reparation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
 
  <%= f.hidden_field :FechaI,:value => Time.now%>
  <%= f.hidden_field :Llamada,:value => 0%>   
  <div class="field">
      <%= f.collection_select  :user_id,User.where(:usertype=> 2).order(:id),:id,:Nombre,prompt: "Seleccione Tecnico"%>
      </div>
  <div class="field">
      <%= f.collection_select  :cproduct_id,Cproduct.order(:id),:id,:NombreC_NombreP,prompt: "Seleccione Producto"%>
      </div>
  <div class="field">
    <%= f.label :Descripcion %><br>
    <%= f.text_area :Descripcion, class: "form-control"%>
  </div>

  <div class="field">
    <%= f.label :PrecioBase %><br>
    <%= f.number_field :PrecioBase, class: "form-control"%>
  </div>

  <div class="actions">
    <%= f.submit "Crear", class: "btn btn-success" %>
  </div>
<% end %>