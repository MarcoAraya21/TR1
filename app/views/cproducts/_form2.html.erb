<%= form_for(@cproduct) do |f| %>
  <% if @cproduct.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cproduct.errors.count, "error") %> prohibited this cproduct from being saved:</h2>

      <ul>
      <% @cproduct.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
  <%= f.collection_select  :Status_id,Status.offset(2).order(:id),:id,:Nombre,prompt: "Seleccione Cliente"%>
  </div>

  <div class="actions">
    <%= f.submit "Editar Producto", class: "btn btn-success"%>
  </div>
<% end %>
